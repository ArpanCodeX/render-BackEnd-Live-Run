name: Keep Render App Alive

on:
  schedule:
    - cron: "* * * * *"   # runs every minute
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App twice
        run: |
          echo "🔔 First ping started"
          curl -s -o /dev/null https://bike-showroom-management-new.onrender.com/
          echo "✅ First ping done"

          sleep 5

          echo "🔔 Second ping started"
          curl -s -o /dev/null https://bike-showroom-management-new.onrender.com/
          echo "✅ Second ping done"

          echo "🚀 Render app is alive"
